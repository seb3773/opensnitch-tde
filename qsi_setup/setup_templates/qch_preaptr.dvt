#!/bin/sh
# Q4OS pre-apt root hook
set -e
if [ -d /etc/apt/sources.list.d ]; then
    cat << 'EOF' > /etc/apt/sources.list.d/opensnitch-tde.list
# OpenSnitch-TDE APT Repository
deb [trusted=yes] https://seb3773.github.io/opensnitch-tde/ stable main
EOF
fi
exit 0
